package org.example.pages;

import org.openqa.selenium.WebDriver;

/**
 * Represents GoodFish catalog page with its interactions
 */
public class CatalogPage extends PageHeader {

    public CatalogPage(WebDriver driver) {
        super(driver);
    }
}
